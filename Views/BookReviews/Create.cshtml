@model LibrarySystem99.Models.BookReview

@{
    ViewBag.Title = "Write a Review";
    var book = ViewBag.Book as LibrarySystem99.Models.Book;
}

<div class="container">
    <h2>Write a Review</h2>

    @if (book != null)
    {
        <p class="lead">
            For <strong>@book.Title</strong> by @book.Author
        </p>
    }

    @using (Html.BeginForm("Create", "BookReviews", FormMethod.Post, new { @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken()

        <input type="hidden" name="BookId" value="@Model.BookId" />

        @Html.ValidationSummary(false, "", new { @class = "text-danger" })

        <div class="form-group">
            <label class="control-label col-md-2">Rating</label>
            <div class="col-md-10">
                <div class="star-rating-input" style="font-size: 32px;">
                    @for (int i = 1; i <= 5; i++)
                    {
                        <span class="star-label" data-value="@i" style="cursor:pointer; color:#ccc; padding: 0 5px;">&#9733;</span>
                    }
                    <input type="hidden" name="Rating" id="ratingValue" value="" />
                </div>
                @Html.ValidationMessage("Rating", "", new { @class = "text-danger" })
                <small class="help-block">Click a star to rate (1 = poor, 5 = excellent).</small>
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2" for="ReviewText">Your Review</label>
            <div class="col-md-10">
                <textarea name="ReviewText" id="ReviewText" class="form-control" rows="5" maxlength="1000" placeholder="What did you think of this book?"></textarea>
                @Html.ValidationMessage("ReviewText", "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn btn-primary">Submit Review</button>
                @Html.ActionLink("Cancel", "Details", "Books", new { id = Model.BookId }, new { @class = "btn btn-default" })
            </div>
        </div>
    }
</div>

@section Scripts {
    <script>
        (function () {
            const stars = document.querySelectorAll('.star-label');
            const hiddenInput = document.getElementById('ratingValue');
            let selected = 0;

            function paint(upTo) {
                stars.forEach((star, i) => {
                    star.style.color = i < upTo ? '#f5a623' : '#ccc';
                });
            }

            stars.forEach((star, i) => {
                star.addEventListener('mouseenter', () => paint(i + 1));
                star.addEventListener('mouseleave', () => paint(selected));
                star.addEventListener('click', () => {
                    selected = i + 1;
                    hiddenInput.value = selected;
                    paint(selected);
                });
            });
        })();
    </script>
}